using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ImageData : MonoBehaviour
{
    public Sprite image; //stores the sprite image for the image to be used
    public float xPos; //stores the x pos value for the image
    public float yPos; //stores the y pos value for the image
    public float width; //stores the width of the image
    public float height; //stores the height of the image

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
